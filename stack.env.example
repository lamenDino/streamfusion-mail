# ── StreamFusion Mail — WireGuard + Environment Config EXAMPLE ──────────────
# Copia questo file in stack.env e compila con le tue credenziali reali.
# Uso: docker compose --env-file stack.env up -d

# ── WireGuard VPN (Gluetun) ───────────────────────────────────────────────────
# Ottieni le credenziali dal tuo provider VPN che supporta WireGuard custom
# (es. Mullvad, ProtonVPN, o un server WireGuard auto-gestito)
VPN_ENDPOINT_IP=
VPN_ENDPOINT_PORT=51820
WIREGUARD_PRIVATE_KEY=
WIREGUARD_ADDRESSES=
WIREGUARD_DNS=
WIREGUARD_PUBLIC_KEY=
WIREGUARD_PRESHARED_KEY=

# ── Addon settings ────────────────────────────────────────────────────────────
NODE_ENV=production
PORT=3000
LOG_LEVEL=info
